# Contracts: test-config

No external contracts are defined for configuration itself. If Specify CLI introduces structured config validation or templating APIs, capture expected schema versions and CLI behaviors here.
